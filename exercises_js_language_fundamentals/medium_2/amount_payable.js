// CLOSURE

// functions retain access to the variables in the enclosing scope.

// Retaining access means that a variable's values is not fixed at the time the function is defined or first executed. Instead, it's value is dynamically looked up each time a function is called.
